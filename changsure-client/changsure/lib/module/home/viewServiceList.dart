import 'package:flutter/material.dart';

class ViewServiceList extends StatelessWidget {
  const ViewServiceList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
